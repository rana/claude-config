# Credentials - NEVER commit
.credentials.json

# Session data (ephemeral, regenerated by Claude Code)
projects/
file-history/
todos/
session-env/
debug/
shell-snapshots/
plans/
ide/
cache/
downloads/
backups/

# Caches and history
stats-cache.json
mcp-needs-auth-cache.json
history.jsonl

# Plugin marketplace cache (downloaded, not authored)
plugins/marketplaces/
plugins/cache/
plugins/install-counts-cache.json
plugins/known_marketplaces.json
plugins/blocklist.json

# Park notes (ephemeral, per-session context)
park-notes/

# Symlinked/generated skills (not authored here)
skills/neon-postgres
